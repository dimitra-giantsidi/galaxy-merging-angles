creating the mask:
> astarithmetic galaxy_name.fits --hdu=0 3 gt --output=galaxy_name_mask.fits
we should mess with:
  - hdu
  - "3"

segmenting:
> astsegment galaxy_name.fits --hdu=0 --detection=galaxy_name_mask.fits --dhdu=1 --std=1 --clumpsnthresh=3.0
we should mess with:
  - hdu (same as as above)
  - dhdu
  - std
  - clumpsnthresh
