#!/bin/bash

echo "Opening up matched filter"
mf="matched_filter/mf.ipynb"

papermill "$mf" "$mf"

if [ $? -eq 0 ]; then
    echo "Success on opening the matched filter!"
else
    echo "Failure on opening the matched filter."
    exit 1
fi
